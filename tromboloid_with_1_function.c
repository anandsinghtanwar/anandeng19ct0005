//Write a program to find the volume of a tromboloid using one function
#include<stdio.h>
Void main()
{
  float h,b,d,vot;
    printf("Enter h:");
    scanf("%f",&h);
    printf("Enter d:");
    scanf("%f",&d);
    printf("Enter b:");
    scanf("%f",&b);
    If (b==0 || h==0 || d==0)
      {
         
         printf (“every parameter show be non zero and positive”);
     }
   else
   {
      vot=(1/(3.0)*((h*d*b)+(d/b))
        printf(“Volume of  tromboloid is :%f\n”,);
   }


}
